<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyecto">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/fonet"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Adivinar"></activity>
        <activity android:name=".Memoria" />
        <activity android:name=".VerFonema" />
        <activity android:name=".BancoFonemas" />
        <activity android:name=".CompararPalabra" />
        <activity android:name=".CompararFonema" />
        <activity android:name=".MiPerfil" />
        <activity android:name=".Juegos" />
        <activity android:name=".MenuPrincipal" />
        <activity android:name=".Registro" />
        <activity android:name=".RecuperarContrasena" />
        <activity
            android:name=".Login"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>